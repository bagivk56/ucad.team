name: GitHub Script
on:
  push:
    branches: [ main ]
jobs:
  deploy:
    steps:
      - name: Build
        uses: bagivk56/ucad.team@main
        with:
          script: ssh ${{ secrets.USER }}@${{ secrets.ADDRESS }} "sh home/c73250/ucad.team.na4u.ru/app/deploy.sh"
